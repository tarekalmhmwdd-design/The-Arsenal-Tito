#!/bin/bash
# تفعيل المسارات يدوياً داخل السكربت لضمان العمل
export PATH="$PATH:/home/root/.local/bin:/usr/local/bin"

echo "--- 🛡️ مشروع الترسانة: جاري الفحص العميق يا تيتو ---"
read -p "أدخل الرقم (بدون +): " target

# 1. فحص الحسابات (نحاول كإيميل افتراضي وكقم)
echo "[+] جاري كشف الارتباطات عبر Holehe..."
holehe "${target}@gmail.com" --only-used 2>/dev/null || echo "[-] فشل فحص Holehe"

# 2. فحص المنصات الاجتماعية
echo "[+] جاري فحص المنصات عبر SocialScan..."
socialscan "$target" --platforms facebook twitter instagram 2>/dev/null

# 3. فحص تسريبات الدارك ويب
echo "[+] جاري البحث في التسريبات عبر h8mail..."
h8mail -t "${target}@gmail.com" -q 2>/dev/null || echo "[-] h8mail غير مستجيب"

echo "--- ✅ تم الانتهاء يا تيتو. راجع results.log ---"
